[package]
name = "load_balancer"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0"
pingora = { version = "0", features = ["lb"] }
base = { path = "../base" }
serde = { version = "1" }
clap = { version = "4", features = ["derive"] }
config = "0"
