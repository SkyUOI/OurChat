[package]
name = "message_logger"
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ourchat-plugin-sdk = { path = "../../../plugin-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
