name: WebMacOS

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main", "dev"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
      - name: Nightly
        run: |
          rustup toolchain install nightly --component miri
          rustup override set nightly
          cargo miri setup
          export MIRIFLAGS="-Zmiri-disable-isolation"
      - name: Build
        run: cd web && cargo build --all
      - name: Build Release
        run: cd web && cargo build --all --release
      - name: Run tests Debug
        run: cd web && cargo test --all
      - name: Run tests Release
        run: cd web && cargo test --all --release
      # - name: Run miri test
      #   run: cargo miri test
